document.getElementById('searchBtn').addEventListener('click', function() {
    var input = document.getElementById('searchInput').value;
    alert('Search for ' + input);
    // Here you can add the logic to search through the tables
});